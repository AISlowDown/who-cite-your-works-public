---
name: who-cite-your-works
description: 检查研究者论文的引用者、重点作者、论文署名机构和引用原文，并生成中文引用影响力报告；适用于手动查询或周期监控。
---

# Who Cite Your Works

查询引用时，先读取用户已确认的论文基线和本地缓存，再访问外部学术数据源。不要把来源失败解释为零引用，也不要根据摘要猜测引用原因。

## 检索前确认范围

每次开始新的手动检索或创建周期监控前，检查用户在当前请求中是否已经明确以下两项。任一项未明确时，必须先在同一条消息中主动询问，取得选择后再访问外部学术数据源；用户已经说明的项目不要重复询问。

1. `作者范围`
   - `第一/通讯作者`：只核验引用论文的第一作者和通讯作者。
   - `全部作者`：引用论文署名作者全部纳入；报告仍标出第一作者、通讯作者和其他共同作者。
2. `机构范围`
   - `重点高校`：只纳入国内 985/211/第二轮“双一流”高校，以及港澳台和国外 QS 前 200 高校的引用。
   - `所有机构`：不按学校删除引用；985/211/第二轮“双一流”、QS 前 200 继续作为重点标签。

把两个选择记录在本次报告的口径说明中。除非用户明确要求保存为以后默认值，否则选择只对当前检索有效；周期监控则把选择固化到该监控任务的提示词或配置中。

## 默认项目数据

- 报告与运行配置：使用用户明确提供的工作目录，或由 `CITATION_RADAR_DATA_DIR` 指定；不在 skill 中写死个人路径。
- 院校判定缓存：`citation-radar/config/institution-priority-cache.json`
- 作者职称与人才称号缓存：`citation-radar/config/author-title-honors-cache.json`
- 版本化缓存快照：本 skill 的 `data/institution-priority-cache-2026-09-01.json`
- 两院院士稳定缓存：本 skill 的 `data/chinese-academicians-cache.json`
- 两院院士当选单位来源登记：本 skill 的 `data/chinese-academician-affiliation-sources.json`
- 中国工程院院士馆公开档案缓存：本 skill 的 `data/chinese-academician-hall-archive-cache.json`
- 国际 Fellow 稳定缓存：本 skill 的 `data/international-fellows-cache.json`
- 国际 Fellow 来源登记：本 skill 的 `data/fellow-source-registry.json`

处理院校优先级前，必须读取 [references/institution-priority-policy.md](references/institution-priority-policy.md)。
处理作者职称、教授身份、人才称号或院士身份前，必须读取 [references/author-title-honors-policy.md](references/author-title-honors-policy.md)。
核验中国科学院或中国工程院院士身份时，还必须读取 [references/chinese-academicians-cache.md](references/chinese-academicians-cache.md)，先查本地缓存再联网。
核验美国国家工程院、英国皇家工程院、加拿大工程院、澳大利亚技术科学与工程院、Royal Society、IEEE、AAAS、IWA、ASCE、AIChE、ACS、RSC 或其他国际科学院/专业学会 Fellow 身份时，还必须读取 [references/international-fellows-cache.md](references/international-fellows-cache.md)，先查本地缓存再联网；缓存未覆盖某组织或姓名未命中都不能作为否定结论。

## 两院院士优先

中国科学院和中国工程院院士是 Honor 核验的第一优先级；国外工程院及国际 Fellow 名单是补充来源，不得因其记录量更大而降低两院院士的检索或展示优先级。

对中国作者或引用论文中出现中国署名机构的作者，先查两院稳定缓存并按“姓名 + 论文署名机构”确认。缓存缺少机构时，以引用论文署名机构为当前场景的优先补全线索，再查两院官网个人页、历次当选名单和作者所在单位官网；国外工程院与国际 Fellow 查询随后执行。最终报告先展示已核验的两院院士身份，再用 ` • ` 追加国外工程院或国际 Fellow 身份。

## 通用知识与特定对象数据分离

- 公开 skill 只保存可复用的检索规则、字段结构、证据优先级、缓存机制和输出样式；不保存特定研究者或引用者的姓名、作者 ID、ORCID、邮箱、私人路径或手工覆盖结果。
- 特定对象的证据和覆盖记录放在用户指定的私有工作目录，由稳定作者 ID 索引；只向通用流程传入输入路径，不再生成包含真实姓名的专用脚本。
- 个案中经过验证的方法应抽象回本 skill；个案结论、人物身份和引用原文仍留在私有 JSON/报告中。

## 核心判定

- 默认剔除目标研究者的作者自引：只要引用论文的完整作者名单中包含目标研究者本人，就排除该引用论文对应的全部引用场景，而不是只删除目标研究者自己的作者行。优先使用稳定作者 ID（OpenAlex Author ID、ORCID）判断，姓名仅用于召回候选并结合机构、研究方向消歧；无法可靠判断时保留原始记录并标为待核验，不能按同名直接删除。报告口径和 JSON 统计必须记录自引剔除规则及剔除数量。
- 高影响作者门槛可配置：用户未指定时默认为作者总被引次数严格大于 2,000；用户指定 `> 5,000` 等门槛时必须覆盖默认值，并保持“严格大于”而不是“大于等于”。报告和 JSON 必须写明实际门槛；保留既有合著者。
- 作者纳入范围服从本次确认结果：选择`第一/通讯作者`时排除普通共同作者；选择`全部作者`时纳入引用论文的所有署名作者并保留角色标签。
- 同一作者跨场景合并后，按作者总被引次数降序排列；作者身份核验默认取前 100 名。原始总被引数、h-index 与数据源标识必须保留，不能把身份核验结果反向改写为新的计量指标。
- 选择`重点高校`时，只保留国内 985/211/第二轮“双一流”高校，以及港澳台和国外当前配置 QS 前 200 高校的引用；国内三类名单满足任一项即可纳入。
- 选择`所有机构`时保留满足作者范围的全部引用场景；985/211、第二轮“双一流”和 QS 前 200 只影响标签与排序，不作为删除条件。
- 作者机构以引用论文上的署名机构为准，不用当前机构替代历史署名机构。优先直接爬取出版商 HTML 的作者—机构对应关系；HTML 不完整时读取论文 PDF 首页/作者信息页；只有无法取得论文原始署名信息时，才回退到 OpenAlex、Crossref 等聚合数据。
- 保存机构原文、规范化机构名、作者—机构对应关系、来源 URL 和来源类型。论文原文与聚合平台冲突时，以论文原文为准并保留冲突备注，不能把聚合平台的作者当前机构覆盖到该引用场景。
- 作者身份搜索至少使用“姓名 + affiliation（引用论文署名机构）”组合，再用研究方向、引用论文题名、官方主页、ORCID 或论文通讯邮箱中的可用信号交叉确认。不能用单独姓名搜索的最高被引实体直接替代论文作者。
- 署名角色按论文原文细分为“唯一第一作者”、“共同第一作者”、“唯一通讯作者”和“共同通讯作者”；共同通讯必须列出其他通讯作者。只有作者贡献声明、共同一作标记或通讯标记能够证明时才填写，不根据作者顺序猜测共同署名。
- 两院院士检索先运行 `node scripts/lookup-academician.mjs <姓名> --affiliation <机构>`。姓名命中只生成院士候选；必须再用论文机构、研究方向或个人标识消歧。同名、跨学部和两院兼任结果不得静默合并或删除。
- 国际 Fellow 检索先运行 `node scripts/lookup-fellow.mjs <姓名> --affiliation <机构>`。本地名单只保存官方目录实际公开的等级与覆盖范围；目录不完整时不得把“未命中”写成“不是 Fellow”。
- 院士/Fellow 先查本地缓存，再按 `node scripts/lookup-academician.mjs <姓名> --affiliation <机构>` 或 `node scripts/lookup-fellow.mjs <姓名> --affiliation <机构>` 双确认。`verified_same_affiliation` 和 `verified_affiliation_change` 可以进入 Honor；`candidate_name_only`、`conflict` 和 `not_found_in_covered_cache` 只保存在证据 JSON。
- 当前机构与论文机构不同不自动判错。只有官方履历加另一身份信号证明正常机构变更时，才能使用 `verified_affiliation_change`；姓名单独命中不能授予荣誉。
- 官方来源刷新失败、403、零行或结果异常小时保留旧缓存和旧记录；部分覆盖或增量覆盖下的未命中不能写成“不是 Fellow”。
- 会员名录以“官方公开最大覆盖”为目标。只有官网公开全量姓名时才能声称完整；只公开年度名单、自愿公开目录或总数时，必须同时输出 `coverageKind`、已覆盖年份/记录数和 `publicLimitations`。
- 两院院士机构不能由毕业院校或历史履历猜测。使用官方个人页丰富字段时，报告必须列出机构覆盖率、`profileFetchStatus` 和失败数；缺少工作单位的记录仍可用姓名召回，但不能伪造机构双确认。
- 两院官网历次当选名单中的工作单位属于“当选时工作单位”。运行 `node scripts/enrich-chinese-academician-affiliations.mjs` 可将其写入 `listedAffiliations`、`careerAffiliations` 和 `affiliationEvidence`，但不得写入 `currentAffiliations` 或表述为当前任职单位。
- 官网只提供图片名单时，可用 `swift scripts/ocr-official-list-image.swift <图片> --tsv` 做文字识别；每行必须再按最终名单的准确姓名和当选年份交叉确认，疑似错字或无法唯一匹配的行不得写入。图片中的工作单位仍只按“当选时工作单位”处理。
- 中科院有效候选人公示表本身不能证明最终当选；只有先与两院最终当选名单交叉确认后，才可把表中工作单位写入对应院士记录，证据类型为 `work-unit-before-election`。它表示增选前同期机构，不是当前机构。
- 原站可访问失败时，可运行 `node scripts/enrich-chinese-academicians-from-hall-archive.mjs` 从 Common Crawl 公开快照恢复中国工程院院士馆官方 PDF。保留原始官方 URL、档案来源/索引和快照日期；主要经历写入 `careerAffiliations`，无结束年月的任职只写入 `currentAtSourceAffiliations`，不得写入 `currentAffiliations`。
- 两院本地缓存未命中不能写成“不是院士”。继续检索中科院/工程院最终当选名单、院士馆或两院个人页、作者所在单位官网，并结合引用论文署名机构和研究方向完成消歧；仍无法确认时保留为“未检出可核验证据”，不得给出否定身份结论。
- 四国工程院名录通过 `node scripts/refresh-international-fellows-cache.mjs --source us-nae --source raeng --source cae-canada --source atse` 增量刷新。美国国家工程院官网中的正式 `Member` 是院士等级，可由该来源单独允许；其他专业学会的普通 `Member` 仍不得进入 Fellow 缓存。
- 院士、会士、会员的判定与中文翻译必须按“组织全名 + 正式等级 + 官方当选/目录证据”处理，再完成机构身份双确认；不能仅按 Member/Fellow 单词判断。展示前读取 [等级判定与中文译名](references/international-fellows-cache.md#等级判定与中文译名)，保留英文原称、中文译名来源及外籍/荣誉等等级区别。
- 退休或荣休状态是可选补充信息，不是必须补全字段。官网自然提供 `Emeritus`、`Retired`、荣休或退休表述时可以原样保存，但不主动为每位院士追查退休状态。
- 不根据年龄、出生年份或当选年份推断退休。已收录的院士/Fellow 不因退休、荣休、`90+` 分组或“资深院士”身份而排除；它们照常参与姓名与机构双确认，确认通过后照常进入 Honor、Excel 和引用影响力报告。

## 引用原文

### Top 10 作者重点引文深挖

先建立完整的引用作者索引和本人论文索引，再集中深挖重点作者。默认验收目标为 **10 位不同的、符合本次筛选条件的作者，每位至少有 1 条核验通过的对应引用原文**；用户可指定其他人数或名单。Top 10 指作者人数，不是论文数、句子数或 Excel 行数。

- 默认按同源、同日期的作者总被引降序选择前 10 位，或按用户指定的优先名单；院士及重要人才称号供选择参考，不自动替代本次筛选门槛。
- 对入选作者，列出其所有已发现且符合筛选条件的引用论文，逐篇尝试获取对应原文，不只取第一篇。先查可复用的已核验缓存，再尝试出版社 HTML/PDF、合法开放版本（包括大学知识库）、开放全文 XML 和可核验引用上下文；聚合接口没有上下文不等于全文不公开，也不等于检索完成。
- 前 10 位中有人暂未取得合格原文，就沿排序继续核查第 11 位及之后的作者，直到累计 10 位达标。补位不删除原先作者；全部作者及其场景仍留在两个完整索引中。未进入深挖轮次的标为“本轮未深挖”，与“已尝试但未取得原文”区分。
- 达标必须同时确认：作者身份和论文署名角色符合本次范围；引用论文确实引用目标论文；引用原句中的参考文献标记或作者—年份对应目标论文。保存作者、该引用论文署名学校、引用论文及链接、本人被引论文及链接、原文、中文解释、来源和获取日期；章节/页码可取得时附上，缺失如实说明。摘要、仅有参考文献条目、未核对对应关系的句子和推测性总结均不计为达标。
- 按已消歧的不同作者计数，同一作者多篇、多句只计 1 人；同一引用论文有多位符合条件作者时可分别计入，但注明共享同一论文证据，不表述为多次独立评价，也不把集体署名论文表述为每个人单独作出的赞誉。
- 达标作者的已发现引用论文也应完成逐篇尝试；达到人数目标后停止新增补位作者。查完当前可确认的合格候选仍不足 10 人时，交付实际人数、目标人数、缺口、已尝试范围和未取得原因。访问限制、额度耗尽或其他阻塞时保留进度并明确未完成，不能声称查尽或达标；不降低门槛、不绕过访问权限、不用摘要或推测凑数。
- 成功缓存不能被空结果覆盖。报告分别统计“已核验原文作者数 / 目标作者数”、不同引用论文数和引用场景数；“同行评价”稿只使用核验通过的原文，区分背景介绍、方法采用、结果比较及明确评价。

运行 `scripts/collect-target-citation-comments.mjs --input <筛选结果.json> --output <引文结果.json> --goal 10 --previous <旧引文结果.json>` 执行补位抓取（首次运行省略 `--previous`）。输入 `strictCandidates` 必须已经完成本次作者、机构、被引门槛和身份筛选。实现覆盖出版社全文链接、PDF 元数据链接、用户提供的合法全文地址、Europe PMC XML、书目信息交叉核验和 Semantic Scholar 分页兜底；详细命令、核验标准及限制见 [references/citation-retrieval.md](references/citation-retrieval.md)。自动定位不等于已经完成中文解读或同行评价稿；尚需按原文审读并回填总结，不能仅凭脚本执行成功就宣称阶段全部完成。

引用上下文按“出版商 HTML/PDF 全文→开放全文 XML（如 Europe PMC/PMC）→可核验的出版商引文上下文→ Semantic Scholar 等聚合引文上下文”顺序取得。对 DOI 和题名双重匹配，保存引用原句、章节/页码、用途分类、中文总结、来源 URL 和获取方式。最终报告中引用原句保留原文且不截断；程序只取得短上下文时，应将其标为“数据源提供的短上下文”，不冒充全文引用段。没有可核验全文或引用上下文时，写明“引用关系已确认，但引用原因暂不可判定”。

## 输出

每次完成检索都必须输出一份中文 Markdown 文本报告，同时保留 JSON 证据数据；需要电子表格时再额外输出 XLSX。生成文本报告前读取 [references/text-report-format.md](references/text-report-format.md)，报告应包含检索口径、结果摘要、作者概览和全部引用场景。每个引用场景保留作者、论文署名机构、作者角色、作者总被引、Title、Honor、引用论文、被引论文、引用原句和中文总结；同一作者引用多篇时逐条列出，不合并。引用原句保持原文，不翻译或截短；多值字段用 ` • ` 分隔。展示层不得出现证据编码、内部 coding 或人工审核过程，来源和细分字段继续保存在 JSON 中。

需要电子表格时，默认在同一个 XLSX 中提供“引用作者索引”和“本人论文索引”两个 Sheet。生成前读取 [references/report-indexes.md](references/report-indexes.md)：前者一位已消歧引用作者一行，后者以已确认的本人论文基线为全集、一篇本人论文一行，并列明哪些学校的哪些老师引用过。两个索引来自同一份筛选后引用场景数据；每个聚合条目保持学校、老师、引用论文、被引论文和引用原文的对应关系。逐条明细继续完整保留在 JSON 和文本报告中，默认不另增明细、核心作者或 Top100 Sheet。用户明确要求单 Sheet 或逐条明细时按本次要求覆盖默认布局。不要新增不透明的综合评分；院士、人才计划和其他荣誉合并在 Honor 中，多项用 ` • ` 分隔，细分字段和证据继续保存在 JSON 中。

当用户要求单 Sheet 作者 Profile 或紧凑展示时，使用紧凑版式，不做整表自动行高：正文建议 8.5–9 pt、表头 9.5–10 pt；表头行高 28–32，普通数据行高 42–48，含可用引用原文的行高 60–68。作者、机构、Title、Honor 列优先保证识别，论文题目、引用原文和中文总结列采用较窄列宽并自动换行。默认列宽可从 `15 / 29 / 29 / 33 / 39 / 39 / 50 / 43` 起步，再通过渲染检查微调。不得为了压缩版面删除或截短单元格原始内容；仅压缩默认显示尺寸，用户仍可在 Excel/WPS 中展开查看完整文字。

生成 XLSX 后必须重新打开 XLSX，至少检查表头和前 10–12 行，并渲染预览图检查列宽、行高、换行和中文显示。同时扫描 `#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?` 和 `#N/A` 等公式错误，以及 OpenAlex/ORCID 等内部证据字段、身份冲突、人工审核、证据编码和 coding 等不应出现在最终展示层的文字。
